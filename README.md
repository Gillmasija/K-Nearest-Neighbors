# K-Nearest-Neighbors
This project implements a K-Nearest Neighbors (KNN) classifier in Python.  It includes data preprocessing, feature scaling, and testing different values of K to find the best model. The results are visualized using an error rate vs K graph.  The project is a simple example of how to use KNN and tune its main parameter.
